# 更新日志

## [1.1.2] - 2026-09-22

### 新增

- 白名单 / 黑名单（配置页「基础 → 权限控制」）。判定顺序：管理员 > 个人白名单 >
  个人黑名单 > 群组白名单 > 群组黑名单；都不命中时白名单启用则拒绝，否则放行。
  管理员用 AstrBot 自带的管理员判定，名单是逗号分隔的 ID
- 覆盖链接解析、`/shot` 与 `/pixiv`；被拦下时静默（只写日志）。网页界面不受名单限制
- 配置页的名单项改用**标签编辑器**（回车添加、点 × 删除，整串粘贴会自动拆分）；
  「禁用的平台」渲染成平台勾选网格。存储格式不变，只是输入方式

## [1.1.1] - 2026-09-22

### 修复

- 小红书 `xiaohongshu.com` 直链（`/explore/<id>`、`/discovery/item/<id>`）全部解析失败，
  报 `IndexError: no such group`。路由表原先按平台域名索引，而小红书在同一个域名下注册了
  两条规则（带 id 的直链、`originalUrl` 安全落地页），后注册的那条覆盖了先注册的，
  于是直链被派发到落地页处理器。路由表改为按规则本身索引，匹配与派发一一对应

## [1.1.0] - 2026-09-21

### 新增

- 管理页面「手动解析」支持整段分享口令，不再要求以 `http://` / `https://` 开头

### 修复

- 配置页改完输入框后，底部「保存更改」按钮不刷新，点了没反应
- 配置页拨动开关会整页重建 DOM，打断正在进行的输入
- 配置页开关的「开启 / 关闭」文案不跟着更新

